<x-layouts.app title="Detail Produk: {{ $product->name }}">
    <div class="container">
        <div class="row">
            <div class="col-md-7">
                <img src="{{ Str::startsWith($product->image, 'http') ? $product->image : asset('storage/' . $product->image) }}" class="img-fluid rounded shadow-sm border" alt="{{ $product->name }}">
            </div>
            <div class="col-md-5">
                <span class="badge bg-warning text-dark mb-2">{{ $product->category->name }}</span>
                <h1 class="mb-3">{{ $product->name }}</h1>
                <p class="h4 text-danger mb-4">Rp {{ number_format($product->price, 0, ',', '.') }}</p>
                
                <h5 class="mt-4">Deskripsi Produk</h5>
                <p class="text-muted">{{ $product->description }}</p>
                
                <div class="d-grid gap-2 mt-4">
                    {{-- Form untuk Tambah ke Keranjang --}}
                    <form action="{{ route('cart.store') }}" method="POST">
                        @csrf
                        <input type="hidden" name="product_id" value="{{ $product->id }}">
                        <button type="submit" class="btn btn-save btn-lg w-100">Tambah ke Keranjang</button>
                    </form>
                    <a href="{{ route('products.index') }}" class="btn btn-outline-secondary">Kembali ke Daftar Produk</a>
                </div>
            </div>
        </div>
    </div>
</x-layouts.app>
